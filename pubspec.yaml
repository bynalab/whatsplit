name: whatsplit
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 23.11.06+4

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  video_trimmer: ^2.1.0
  multi_image_picker: ^4.8.01
  # share_plus: ^7.0.2
  share_plus: ^4.0.10
  cupertino_icons: ^1.0.2
  helpers: ^1.2.3
  image_picker: ^0.8.9
  flutter_video_info: ^1.3.1
  google_fonts: ^4.0.3
  # http: ^0.13.4

dev_dependencies:
  # flutter_test:
  #   sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/

  fonts:
    - family: Selima
      fonts:
        - asset: assets/font/selima/Selima.ttf
